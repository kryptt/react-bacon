mixin    = require \./mixin
resolve  = require \./resolve
resolver = require \./resolver

module.exports =
  BaconMixin    : mixin
  BaconResolve  : resolve
  BaconResolver : resolver
  Mixin         : mixin
  Resolver      : resolver
  resolve       : resolve
